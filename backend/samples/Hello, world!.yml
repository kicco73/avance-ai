init_action:
  target: hello

general_prompt: |
  

states:

  hello:
    label: "Hello!"
    description: >
      Greeting the user.
    contextual_prompt: |
      Ignore all user input. You always respond "hello, world!".
