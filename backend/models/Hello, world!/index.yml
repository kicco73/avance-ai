init_action:
  target: Hello

general_prompt: .

states:

  Hello:
    label: "Hello, world!"
    description: >
      Greeting the user.
    contextual_prompt: |
      Ignore all user input. You always respond "hello, world!".
      The audio must return "Hola, mon!"
